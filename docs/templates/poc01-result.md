# POC01 结果记录（复制到 .local/，不要提交原始生产信息）

## 实验身份

执行日期/时区：
仓库commit / 脚本SHA256：
Spark系统/架构、空闲内存、业务负载：
环境快照路径：
模型服务地址（仅回环）、/models准确ID：
权重来源/版本或revision、量化：
推理服务版本、容器镜像tag/digest：
上下文长度、KV/内存预算、并发、前缀缓存：
聊天模板、工具解析器、自动工具选择设置：
推理解析器、实际thinking设置及验证证据（未知写UNKNOWN）：
request参数、每轮输出上限、任务总时限：
模型是否已加载、预热/冷启动如何标记：
本次与上次唯一改变量：
假设/反方解释/什么结果能否定假设：

## 用例范围

合成数据版本与hash：
真实原日志是否可得、hash、原始指令是否完整：
真值由谁以什么方式独立核对：
任务输出约定相对原POC的变化：
文件总数/字节、图片数量/分辨率：
缺失条件与未经验证事项：

## 分类结果

| 用例 | 计划/已尝试 | 正确次数 | 120秒内正确 | 中位/P90 wall秒 | 模型轮数/工具数 | 运行目录 |
|---|---|---|---|---|---|---|
| direct-basic | | | | | | |
| tool-basic | | | | | | |
| direct-empty | | | | | | |
| tool-empty | | | | | | |
| chain | | | | | | |
| vision | | | | | | |
| vision-swap | | | | | | |
| 原日志direct | | | | | | |
| 原日志tool | | | | | | |

预热不并入；人工干预与取消单列；未运行不算成功。五次样本不足以保证总体95%可靠性。

## 首个失败与证据

失败阶段：配置 / 连接 / 模型输入 / 工具解析 / 参数 / 工具结果回传 / JSON格式 / 事实错误 / 停止条件 / token上限 / 总超时 / 视觉 / 其他。

对应request/response/events路径：
实际表现（先事实）：
已排除的替代解释与依据：
仍未知的原因：
服务端是否还在生成、业务是否受到影响：

## 结论

合成文本阶段门：通过 / 不通过 / 未完整执行。
基础双图：通过 / 不通过 / 未执行。
原失败日志：通过 / 不通过 / 材料不足尚未复现。
是否值得进入POC02（不等于产品通过）：
下一轮只改变什么、为什么、预计看什么证据：

对外反馈只分享人工脱敏摘要，不上传本地原始文件夹。
